import styled from 'styled-components';

const TodoList = styled.ul`
  width: 100%;
  height: 70vh;
  overflow: auto;
  position: relative;
  bottom: -25px;
  list-style: none;
  padding-right: 40px;
`;

export default TodoList;
